name: sentry-test

services:
  sentry-test:
    build:
      context: .
      dockerfile: Dockerfile
    user: ${UID:-1000}:${GID:-1000}
    volumes:
      - ./:/app
      - /etc/passwd:/etc/passwd:ro
      - /etc/group:/etc/group:ro
    env_file:
      - .env
    stdin_open: true
    tty: true
